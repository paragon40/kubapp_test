apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.postgres.name }}
  namespace: {{ .Values.namespace }}

spec:
  selector:
    app: {{ .Values.postgres.name }}

  ports:
    - port: {{ .Values.service.port }}
      targetPort: 5432

  type: ClusterIP
